const $=s=>document.querySelector(s);const error=$('#error');
$('#togglePassword').onclick=()=>{const i=$('#password');i.type=i.type==='password'?'text':'password';$('#togglePassword').textContent=i.type==='password'?'Show':'Hide'};
$('#loginForm').onsubmit=async e=>{e.preventDefault();error.hidden=true;$('#loginBtn').disabled=true;$('#loginBtn').textContent='Signing in...';try{const r=await fetch('/api/teacher/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:$('#username').value.trim(),password:$('#password').value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to sign in.');location.href='dashboard.html'}catch(err){error.textContent=err.message;error.hidden=false;$('#loginBtn').disabled=false;$('#loginBtn').textContent='Sign in to Teacher Portal'}};
